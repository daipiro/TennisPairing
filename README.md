# テニス乱数表 Webアプリ（PWA）

テニスコート1面で4〜8人のメンバーが継続的にダブルスを行う際、過去の確定履歴を考慮してペアや対戦相手の重複を避けた最適な組み合わせを生成するスマートフォン専用のWebアプリケーションです。

Progressive Web App (PWA) に対応しており、一度アクセスすればオフライン環境（テニスコート上など）でも快適に利用可能です。

---

## 🌟 主な機能

### 1. 参加人数の柔軟な設定
- **4〜8人の参加者に対応**: 参加者の表記は `1` 〜 `N` のシンプルで直感的な番号表示です。
- **自動休憩人数計算**: 休憩者数は常に `参加人数 - 4人` となります。

### 2. シームレスなメイン1画面操作 & リアルタイム時系列対戦履歴
- **組み合わせ作成・確認画面の遷移なし**: 別画面へ移動することなく、メイン画面内で組み合わせ確認（コート表示）、再抽選、手動入れ替え、確定まで完結します。
- **メイン画面上部への時系列履歴表示**: 上部に過去の対戦履歴（第1ゲームが一番上、下へ追加）がリアルタイム表示されます。
- **画面全体縦スクロール**: 画面全体をスクロールして上部で全試合履歴を確認し、下部で即座に「この組み合わせで確定」を押せます。

### 3. 休憩者のオプション設定・選択状態の保持
- **休憩者のオプション画面指定**: 休憩する人の手動選択をメインフローから独立した「オプション画面」で行えます。
- **選択状態の永続保持**: オプション画面で設定した休憩者指定は `localStorage` に保持され、ユーザーが変更するまで次回以降のゲームでも継続適用されます。
- **自動ランダム補充**: 手動で指定していない分や不足分がある場合は、アプリが自動でランダムに休憩者を選出します。

### 4. 公平性評価アルゴリズムに基づく組み合わせ自動生成
過去の確定履歴から以下の優先順位でペナルティスコアを計算し、重複を極力避ける最良のペア・対戦カードを提案します。
1. ペア重複の回避（100pt/回）
2. 対戦相手重複の回避（10pt/回）
3. 同一対戦カードの回避（30pt/回）
4. 直前ゲームのペア回避（300pt/回）・直前ゲーム対戦カード回避（500pt）
5. 再抽選で直前表示と同一カードの回避（200pt）

### 5. 再抽選 & 出場者の手動位置入れ替え
- **再抽選**: 休憩者は変更せず、出場する4人のペア構成のみを公平に再生成できます。
- **手動位置入れ替え**: コート上のプレイヤー番号を2人タップするだけで、位置を自由に入れ替えられます。

### 6. 履歴管理・直前取り消し・個人統計
- **ゲーム確定と保存**: 確定したゲームは `localStorage` に即時保存され、ブラウザを閉じても維持されます。
- **直前の確定取り消し**: 誤って確定した場合でも、最新1ゲームの確定を取り消して状態を巻き戻すことができます。
- **参加状況統計**: メンバーごとの累計「出場回数」「休憩回数」をリアルタイムで確認できます。

### 7. PWA & オフライン対応
- **ホーム画面へ追加**: スマートフォンのブラウザからアプリとしてインストール可能です。
- **オフライン動作**: 初回アクセス後はネット接続なしでも全機能が動作します。

---

## 🛠️ 技術スタック

| 項目 | テクノロジー |
|---|---|
| 開発環境 | Vite 6 |
| 言語 | Vanilla JavaScript (ES Modules) |
| UIスタイル | Tailwind CSS v4 |
| PWA実装 | vite-plugin-pwa (Workbox) |
| データ保存 | localStorage (端末内保存) |

---

## 🚀 使い方・開発コマンド

### 動作要件
- Node.js (v18 以上推奨)
- npm

### セットアップ

```bash
# リポジトリのクローン・移動後、依存パッケージをインストール
npm install
```

### 開発サーバーの起動

```bash
npm run dev
```

起動後、ターミナルに表示されるローカルURL（例: `http://localhost:5173`）にアクセスしてください。

### プロダクションビルド & PWA生成

```bash
npm run build
```

`dist/` ディレクトリに最適化された静的ファイルおよび PWA サービスワーカーが生成されます。

### ビルド成果物のローカル確認

```bash
npm run preview
```

---

## 📱 画面フロー

1. **開始画面**: 参加人数 (4〜8人) を選択して開始。
2. **メイン画面（1画面完結型）**:
   - **上部**: これまでの対戦履歴（第1ゲームが一番上、下へ追加）。
   - **中央**: 現在作成された第 N ゲームの対戦カード（コート表示）。タップで2人手動入れ替え、再抽選が可能。
   - **下部**: 「この組み合わせで確定 (第 N ゲーム)」ボタン。押すと確定され、その場で次の第 N+1 ゲームの組み合わせが即座に生成表示されます。
3. **休憩者指定オプション画面**: 毎ゲーム固定で休憩させたい人を手動設定。選択状態は次回以降も保持されます。
4. **履歴・参加状況詳細画面**: 累計の出場・休憩回数、直前取り消しやデータリセット。

---

## 📝 変更履歴 (Changelog)

### ver 1.3.0
- 別画面への遷移を廃止し、組み合わせ作成・確認・再抽選・手動入れ替え・確定をすべてメイン画面内に表示・完結する1画面シームレスUIに変更。
- 確定ボタンを押すと画面遷移せずにその場で履歴が追加され、次回ゲームの組み合わせが即時生成表示されるように最適化。

### ver 1.2.0
- メイン画面上部に対戦履歴表示を追加（一番上が第1ゲームで、下へ順に追加される時系列表示）。
- 画面全体でスクロールできるレイアウトに変更。

### ver 1.1.0
- 休憩者の選択画面をメインフローから独立した「オプション画面」に変更。
- オプション画面で設定した休憩者の選択状態を保持（`localStorage` に永続化）する機能を追加。

### ver 1.0.0
- 初版リリース。
