# テニス乱数表アプリ 要件定義書（改訂版）

## 1. 概要

4〜8人で1面のテニスコートを使用してダブルスを継続的に行う、スマートフォン専用のWebアプリ。

各ゲームで休憩者を手動指定できるほか、指定しない場合はアプリがランダムに休憩者を決定する。休憩者を除いた4人について、過去の確定履歴を考慮し、ペアや対戦相手の重複をできるだけ避ける組み合わせを生成する。

試合結果・得点は扱わない。

## 2. 技術要件

| 項目 | 仕様 |
|---|---|
| 開発環境 | Vite |
| 言語 | Vanilla JavaScript |
| UIスタイル | Tailwind CSS |
| アプリ形式 | Progressive Web App（PWA） |
| PWA実装 | `vite-plugin-pwa` を使用 |
| データ保存 | `localStorage` を用いた端末内保存 |
| 対象端末 | スマートフォン専用 |
| 通信 | 初回アクセス時は必要。以後はオフラインで基本機能を利用可能 |

## 3. 基本仕様

| 項目 | 仕様 |
|---|---|
| 参加人数 | 4〜8人 |
| 参加者表記 | `1`〜`8` の番号のみ |
| コート数 | 1面固定 |
| 種目 | ダブルスのみ |
| 1ゲームの出場者 | 4人 |
| 試合数 | ユーザーが終了するまで継続 |
| 試合結果 | 記録しない |

休憩者数は常に `参加人数 - 4人` とする。

| 参加人数 | 出場 | 休憩 |
|---:|---:|---:|
| 4人 | 4人 | 0人 |
| 5人 | 4人 | 1人 |
| 6人 | 4人 | 2人 |
| 7人 | 4人 | 3人 |
| 8人 | 4人 | 4人 |

## 4. 利用フロー

```text
参加人数を選択
 ↓
休憩者を任意で選択
 ↓
組み合わせを作成
 ├─ 未選択・不足分あり：残りの休憩者をランダム決定
 └─ 必要人数を選択済み：選択内容をそのまま採用
 ↓
組み合わせを確認
 ↓
再抽選または手動変更
 ↓
組み合わせを確定
 ↓
履歴・統計を更新
 ↓
次のゲームへ
```

## 5. 休憩者の指定・自動決定

### 5.1 手動指定

- 各ゲームの開始前に、休憩者を任意で選択できる。
- 手動で選択した参加者は、必ずそのゲームで休憩する。
- 最大選択人数は `参加人数 - 4人` とする。
- 選択人数が上限に達した場合、それ以上は選択できない。

### 5.2 未選択時のランダム決定

- 休憩者を1人も選択しなくても、「組み合わせを作成」を実行できる。
- 必要な休憩者全員を、参加者からランダムに選ぶ。
- 例：6人参加で誰も指定しない場合、6人からランダムに2人を休憩者にする。

### 5.3 一部だけ指定した場合

- 必要人数より少ない人数だけ指定した場合、指定者は休憩とし、不足人数のみランダムに選ぶ。
- 例：7人参加で「1」だけを休憩指定した場合、残り6人からランダムに2人を追加で休憩者にする。
- 自動決定された休憩者は、組み合わせ確認画面で「自動選択」と分かる表示を行う。

### 5.4 ランダム休憩の確定タイミング

- 休憩者の自動決定は「組み合わせを作成」を押した時点で行う。
- 組み合わせの再抽選では、休憩者は変更しない。
- 休憩者を変更したい場合は「休憩者を選び直す」へ戻る。
- 確定前の自動決定休憩者は履歴に保存しない。
- 組み合わせ確定時に、手動指定・自動選択を問わず、最終的な休憩者を履歴へ保存する。

## 6. 組み合わせ生成

休憩者を除く4人を対象に、以下の3パターンから1つを選ぶ。

```text
A・B vs C・D
A・C vs B・D
A・D vs B・C
```

生成では、確定済みゲームの履歴のみを評価対象とする。再抽選中・手動変更中の未確定組み合わせは、履歴として扱わない。

## 7. 公平性評価アルゴリズム

休憩者の手動指定は絶対条件とする。残った4人のペア構成に対し、以下の順で公平性を評価する。

1. 同じペアの繰り返しを避ける
2. 同じ相手との対戦の繰り返しを避ける
3. 同じペア対ペアの対戦カードを避ける
4. 直前ゲームのペア・対戦カードを特に避ける
5. 同点候補にはランダム性を持たせる

### 7.1 保存・集計する履歴

各確定ゲームについて、以下を保存する。

```js
{
  gameNumber: 3,
  team1: [1, 6],
  team2: [3, 5],
  restPlayers: [2, 4],
  manuallySelectedRestPlayers: [2]
}
```

履歴から以下を集計する。

- 参加者ごとの出場回数・休憩回数
- 2人がペアとなった回数
- 2人が敵同士となった回数
- 同一対戦カードの発生回数

### 7.2 候補スコア

各候補には、低いほど良いペナルティスコアを設定する。

```text
候補スコア =
  ペア重複回数 × 100
+ 対戦相手重複回数 × 10
+ 同一対戦カード回数 × 30
+ 直前ゲームと同じペア × 300
+ 直前ゲームと同じ対戦カード × 500
+ 再抽選で直前表示と同一 × 200
```

### 7.3 最終候補の選択

1. 3つの候補を生成する。
2. 各候補のペナルティスコアを計算する。
3. 最小スコアから一定範囲内の候補を抽出する。
4. 抽出候補からランダムに1つを選ぶ。

最小スコアだけに固定せず、軽微な差の候補からランダムに選ぶことで、毎回同じ流れになることを防ぐ。

## 8. 再抽選・手動変更・確定

### 8.1 再抽選

- 確定前に何度でも実行できる。
- 休憩者は変更せず、4人のペア・対戦構成だけを再生成する。
- 直前に表示した組み合わせには再表示ペナルティを加える。
- 再抽選結果は、確定するまで履歴へ反映しない。

### 8.2 手動変更

- 組み合わせ中の参加者番号を1人ずつタップする。
- 2人を選ぶと、両者の表示位置を入れ替える。
- 手動変更した内容を優先し、公平性スコアによる警告・制限は行わない。
- 休憩者を出場側へ入れ替えることはできない。

### 8.3 確定

- 「この組み合わせで確定」を押すと、ゲーム履歴を登録する。
- ペア・対戦・出場・休憩の統計を更新する。
- 確定後は、次のゲームの休憩者選択画面へ進む。

### 8.4 直前の確定取り消し

- 最新の確定ゲームを取り消せる。
- 取り消したゲームの履歴および統計は自動で巻き戻す。
- 取り消したゲームの休憩者・組み合わせを復元し、再抽選・手動変更・再確定できる。
- 誤操作防止の確認ダイアログを表示する。
- 初版では直前の1ゲームのみ取り消し対象とする。

## 9. 画面仕様

### 9.1 開始画面

```text
テニス乱数表

参加人数
[ 4人 ] [ 5人 ] [ 6人 ]
[ 7人 ] [ 8人 ]

[ 開始する ]
```

### 9.2 休憩者選択画面

```text
第3ゲーム

休憩する人を選択
選択しない場合はランダムに決定します

[ 1 ] [ 2 ] [ 3 ]
[ 4 ] [ 5 ] [ 6 ]

選択中：1人 ／ 最大2人
不足分：ランダムに1人選択

[ 組み合わせを作成 ]
```

- 「組み合わせを作成」は常に有効。
- 4人の場合は休憩者を選択せず、全員出場として直接組み合わせを作成する。

### 9.3 組み合わせ確認画面

```text
第3ゲーム

     2 ・ 4
        VS
     3 ・ 6

休憩：1、5
手動指定：1 ／ 自動選択：5

[ 再抽選 ]
[ 組み合わせを変更 ]
[ この組み合わせで確定 ]

[ 休憩者を選び直す ]
```

### 9.4 履歴画面

```text
履歴・参加状況

第3ゲーム
2・4 VS 3・6
休憩：1、5

参加状況
1：出場 2回 ／ 休憩 1回
2：出場 3回 ／ 休憩 0回

[ 直前の確定を取り消す ]
[ 最初からやり直す ]
```

## 10. 将来拡張候補

- 参加者名の設定
- 休憩者の自動選択時に、休憩回数の少ない人を優先する公平性設定
- 試合結果・勝敗・得点の記録
- 複数コート対応
- 途中参加・途中離脱
- 履歴の編集・個別削除
- CSV出力
- PWA対応およびオフライン利用
- ペア・対戦重複の詳細統計

## 11. PWA機能要件

### 11.1 ホーム画面への追加

- スマートフォンのブラウザから、アプリをホーム画面へ追加できる。
- ホーム画面から起動した場合は、通常のWebページではなくアプリとして表示する。
- アプリ名は「テニス乱数表」とする。
- アプリ用アイコンを設定する。
- 起動時のテーマカラーはコートをイメージした緑色とする。

### 11.2 オフライン利用

- 初回アクセス後は、通信環境がない場所でもアプリを起動できる。
- 以下の機能はオフラインで利用可能とする。

  - 参加人数の選択
  - 休憩者の手動指定・ランダム選択
  - 組み合わせ生成・再抽選
  - 手動入れ替え
  - 組み合わせ確定
  - 履歴閲覧
  - 直前の確定取り消し
  - リセット

- オフライン中も、履歴および進行中のゲーム状態を端末内に保存する。

### 11.3 キャッシュ対象

Service Workerにより、以下をキャッシュする。

- アプリのHTML
- JavaScriptバンドル
- CSS
- アプリアイコン
- Web App Manifest
- フォントなど、アプリ起動に必要な静的ファイル

通信を必要とする外部APIやサーバー機能は初版では使用しない。

### 11.4 更新時の動作

- 新しいバージョンが公開された場合、バックグラウンドで更新を検知する。
- 次回起動時、またはユーザーが更新を選択した時点で新しいバージョンを適用する。
- 更新時も `localStorage` 上の履歴・ゲーム進行データを維持する。
- アプリ画面上に「新しいバージョンがあります。更新する」通知を表示できるようにする。

### 11.5 マニフェスト仕様

Web App Manifestには、少なくとも以下を設定する。

```json
{
  "name": "テニス乱数表",
  "short_name": "テニス乱数表",
  "display": "standalone",
  "start_url": "/",
  "theme_color": "#146c5a",
  "background_color": "#eef9f4",
  "lang": "ja"
}
```

アイコンは、最低でも以下のサイズを用意する。

- 192 × 192 px
- 512 × 512 px

### 11.6 データの扱い

- ゲーム履歴・参加状況・未確定の組み合わせは端末内にのみ保存する。
- 初版では、サーバー送信・アカウント登録・他端末同期は行わない。
- ブラウザデータを削除した場合、保存済み履歴も削除される。
- 将来的にCSV出力やクラウド同期を追加できる設計とする。